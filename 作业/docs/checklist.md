# 验收清单

- [ ] 使用 conda 创建环境，没有使用 `venv`、`virtualenv` 或全局 Python。
- [ ] 提交了原始 `requirements_broken.txt`。
- [ ] 保存了安装失败日志 `logs/install_failed.log`。
- [ ] 记录了操作系统、Python 版本和 GPU 情况。
- [ ] 提交了 `conflict_analysis.md`，至少解释 4 个依赖问题。
- [ ] 提交了 `environment_fixed.yml` 和 `requirements_fixed.txt`。
- [ ] 提交了可运行的 `check_env.py`。
- [ ] 提交了 `fix_strategy.md`，包含最小修复和工程修复两套方案。
- [ ] README 写清楚运行步骤、环境信息和验证方式。
- [ ] 没有提交 `.conda/`、`envs/`、大型缓存目录或个人绝对路径。
